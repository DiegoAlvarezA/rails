<%= form_with(model: linea_item, local: true) do |form| %>
  <% if linea_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(linea_item.errors.count, "error") %> prohibited this linea_item from being saved:</h2>

      <ul>
      <% linea_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id %>
  </div>

  <div class="field">
    <%= form.label :order_id %>
    <%= form.text_field :order_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
